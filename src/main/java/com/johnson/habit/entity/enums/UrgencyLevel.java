package com.johnson.habit.entity.enums;

public enum UrgencyLevel {
    BUILD_HABIT,        // starting something new
    BREAK_HABIT,        // stopping something bad
    IMPROVE_CONSISTENCY,// already doing it but inconsistently
    ACHIEVE_MILESTONE
}
